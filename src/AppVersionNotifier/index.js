import React, { PropTypes } from 'react'

const propTypes = {}
const defaultProps = {}

const AppVersionNotifier = () => {
  return <div>AppVersionNotifier</div>
}

AppVersionNotifier.propTypes    = propTypes
AppVersionNotifier.defaultProps = defaultProps

export default AppVersionNotifier
